<%= form_for(@history_article) do |f| %>
  <% if @history_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history_article.errors.count, "error") %> prohibited this history_article from being saved:</h2>

      <ul>
      <% @history_article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
