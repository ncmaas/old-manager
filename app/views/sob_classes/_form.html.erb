<%= form_for(@sob_class) do |f| %>
  <% if @sob_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sob_class.errors.count, "error") %> prohibited this sob_class from being saved:</h2>

      <ul>
      <% @sob_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.number_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
